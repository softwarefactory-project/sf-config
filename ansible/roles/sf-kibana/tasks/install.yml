---
- include_tasks: configure_mirror.yml

- name: Install kibana package
  yum:
    name: "kibana-{{ elk_stack_version }}.{{ elk_sub_version }}.0-1"
    state: present
    disablerepo: "*"
    enablerepo: "{{ elastic_repo_name }}"
