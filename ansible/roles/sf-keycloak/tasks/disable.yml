---
- name: Disable services
  service:
    name: keycloak
    state: stopped
    enabled: "no"
  ignore_errors: "yes"

- name: Delete containers
  include_role:
    name: sf-container
    tasks_from: delete.yaml
  loop: "{{ keycloak_components }}"
