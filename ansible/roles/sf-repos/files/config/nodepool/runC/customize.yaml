---
# Tasks in that files are executed on the hypervisor-oci node by the config-update
- include_tasks: _linters-packages.yaml

- include_tasks: rootfs-fedora-29.yaml

- include_tasks: rootfs-fedora-rawhide.yaml

- include_tasks: rootfs-centos-7.yaml

# User defined containers and customizations
- include_tasks: "{{ item }}"
  with_fileglob:
    - custom/*.yaml
