Upload pages to a static webserver

This uploads pages to a static webserver using SSH.

**Role Variables**

.. zuul:rolevar:: zuul_pagesserver_root
   :default: /var/www/pages/

   The root path to the pages on the pages server.
