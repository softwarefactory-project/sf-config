---
- job:
    name: base
    parent: null
    description: The base job.
    pre-run: playbooks/base/pre
    post-run:
      - playbooks/base/post
    roles:
      - zuul: zuul-jobs
    timeout: 1800
    # Set attempts to 1 until it's working well
    attempts: 1
    secrets:
      - site_sflogs
    nodeset:
      nodes:
        - name: main
          label: centos-oci

- job:
    name: linters
    parent: base
    description: A bash8, pip8, yamllint linter check.
    run: playbooks/linters/run

- project:
    name: zuul-jobs
    check:
      jobs:
        - linters
    gate:
      jobs:
        - linters

{% if 'zuul' not in roles %}
- job:
    name: config-check
    parent: base
    run: playbooks/config/check
    nodeset:
      nodes:
        - name: main
          label: local-centos-oci

- job:
    name: config-update
    parent: base
    run: playbooks/config/update
    secrets:
      - site_install_server
    nodeset:
      nodes:
        - name: main
          label: local-centos-oci

- project:
    name: config
    check:
      jobs:
        - config-check
    gate:
      jobs:
        - config-check
    post:
      jobs:
        - config-update
{% endif %}
