---
- hosts: localhost
  roles:
    - block:
        - import_role: name=emit-job-header
        - import_role: name=log-inventory
      vars:
        zuul_log_url: "https://{{ fqdn }}/logs"

- hosts: all
  roles:
    - prepare-workspace
    - role: validate-host
