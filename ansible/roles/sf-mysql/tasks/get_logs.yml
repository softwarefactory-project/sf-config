---
- name: Dump mysql db
  shell: "mysqldump --all-databases | sed 's$VALUES ($VALUES\n($g' | sed 's$),($),\n($g' | gzip > /var/log/mariadb/dump.gz"

- name: "Fetch mariadb logs"
  synchronize:
    mode: pull
    src: /var/log/mariadb/
    dest: "{{ log_dest }}/log"
  ignore_errors: true
