---
- block:
    - name: Ensure zuul service is disabled
      service:
        name: zuul
        state: stopped
        enabled: "no"
      ignore_errors: "yes"
  when: sf_previous_version == "2.5.0"
