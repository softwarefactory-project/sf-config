---
role_actions:
  - install
  - setup
  - disable
  - backup
  - restore

role_package: zookeeper-lite

zookeeper_tickTime: 5000
# Large min/max session timeout to mitigate network outage between zuul and zk
zookeeper_minSessionTimeout: 600000
zookeeper_maxSessionTimeout: 1800000
