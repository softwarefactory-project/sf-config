---
# TODO: fix zookeeper backup to only include what is needed
#- name: "Backup data"
#  synchronize:
#    mode: pull
#    src: "{{ item }}"
#    dest: "{{ backup_dest }}/"
#    delete: "yes"
#    rsync_opts: --relative
#  loop:
#    - /var/lib/zookeeper/version-2
