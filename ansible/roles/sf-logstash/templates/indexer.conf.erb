input {
  tcp {
    host => "logstash.{{ fqdn }}"
    port => 9999
    codec => json_lines {}
    type => "jenkins"
  }
}

# filter {}

output {
  elasticsearch {
    hosts => ['elasticsearch.{{ fqdn }}:9200']
  }
}
