input {
  tcp {
    host => "{{ logstash_host }}"
    port => "{{ logstash_input_port }}"
    codec => json_lines {}
    type => "jenkins"
  }
}

# filter {}

output {
  elasticsearch {
    hosts => ['{{ elasticsearch_host }}']
  }
}
