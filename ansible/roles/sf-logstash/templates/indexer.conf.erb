input {
  tcp {
    host => "{{ logstash_host }}"
    port => 9999
    codec => json_lines {}
    type => "jenkins"
  }
}

# filter {}

output {
  elasticsearch {
    hosts => ['{{ elasticsearch_fqdn }}']
    flush_size => 1024
  }
}
