---
- block:
    - name: "Remove edeploy configuration"
      file:
        path: "{{ item }}"
        state: absent
      with_items:
        - /var/lib/edeploy
  when: sf_previous_version == "2.5.0"
