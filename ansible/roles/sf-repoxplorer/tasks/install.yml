---
- name: "Install package"
  yum:
    name: "repoxplorer"
    state: present
    disablerepo: "{{ yum_disable_repo|default(omit) }}"
    enablerepo: "{{ yum_enable_repo|default(omit) }}"
  delegate_to: "{{ item }}"
  with_items: "{{ groups['zuul-executor'] | union( groups['repoxplorer'] ) }}"
