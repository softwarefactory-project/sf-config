---
- hosts: all
  tasks:
    - name: Upload logs.
      command: "rsync -r --chmod=Duog+rx,Fuog+r {{ zuul.executor.log_root }}/ loguser@LOGSERVER:/var/www/logs/{{ zuul.uuid }}/"
      delegate_to: 127.0.0.1
