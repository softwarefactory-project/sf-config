- project-template:
    name: sf-jobs
    check:
      jobs:
        - linters
        - sf-rpm-build
        - sf-ci-functional-tenant:
            dependencies:
              - sf-rpm-build

- project:
    name: software-factory/sf-config
    templates:
      - sf-jobs
